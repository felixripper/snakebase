# ==============================================================================
# SNAKEBASE - Environment Variables
# ==============================================================================
# Bu dosyayı kopyalayıp .env.local olarak kaydedin ve değerlerinizi girin
# Copy this file to .env.local and fill in your values
# ==============================================================================

# ------------------------------------------------------------------------------
# Next.js Public Variables (Tarayıcıda görünür / Visible in browser)
# ------------------------------------------------------------------------------

# Uygulamanızın public URL'i (örn: https://snakebase.vercel.app)
# Your application's public URL (e.g., https://snakebase.vercel.app)
NEXT_PUBLIC_URL=http://localhost:3000

# Coinbase OnchainKit API Key (https://portal.cdp.coinbase.com/)
# Get your API key from Coinbase Developer Platform
NEXT_PUBLIC_ONCHAINKIT_API_KEY=your_onchainkit_api_key_here

# ------------------------------------------------------------------------------
# Admin Panel Authentication (Sunucu tarafı / Server-side only)
# ------------------------------------------------------------------------------

# Admin paneli kullanıcı adı
# Admin panel username
ADMIN_USERNAME=admin

# Admin paneli şifresi (Güçlü bir şifre kullanın!)
# Admin panel password (Use a strong password!)
ADMIN_PASSWORD=change_this_to_a_strong_password

# Session cookie şifreleme anahtarı (En az 32 karakter, rastgele olmalı!)
# Session cookie encryption key (At least 32 characters, must be random!)
# Oluşturmak için: openssl rand -base64 32
# To generate: openssl rand -base64 32
SECRET_COOKIE_PASSWORD=your_secret_cookie_password_here_at_least_32_characters_long

# ------------------------------------------------------------------------------
# Upstash Redis (Oyun konfigürasyonu için / For game configuration storage)
# ------------------------------------------------------------------------------
# Ücretsiz Redis: https://upstash.com/
# Free Redis database: https://upstash.com/

# Upstash Redis REST URL (https:// ile başlamalı)
# Upstash Redis REST URL (must start with https://)
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io

# Upstash Redis REST Token
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_here

# Alternatif: Eğer Redis kullanmak istemiyorsanız, bu değişkenleri boş bırakın
# In-memory (geçici) depolama kullanılacaktır
# Alternative: Leave these empty to use in-memory storage (temporary)

# ------------------------------------------------------------------------------
# Blockchain / Smart Contract (Opsiyonel / Optional)
# ------------------------------------------------------------------------------

# Base Mainnet RPC URL (örn: https://mainnet.base.org)
# Base Mainnet RPC URL (e.g., https://mainnet.base.org)
BASE_MAINNET_RPC=https://mainnet.base.org

# Smart contract deploy için private key (ASLA paylaşmayın!)
# Private key for contract deployment (NEVER share this!)
# DEPLOY_PRIVATE_KEY=your_private_key_here

# Deploy edilmiş GameScore contract adresi
# Deployed GameScore contract address
# GAME_SCORE_CONTRACT_ADDRESS=0x...

# ------------------------------------------------------------------------------
# Development / Production (Otomatik / Automatic)
# ------------------------------------------------------------------------------
# Bu değişkenler Vercel tarafından otomatik olarak ayarlanır
# These variables are automatically set by Vercel

# NODE_ENV=development
# VERCEL_ENV=production
# VERCEL_URL=your-project.vercel.app

# ------------------------------------------------------------------------------
# Sentry Error Tracking (Opsiyonel / Optional)
# ------------------------------------------------------------------------------
# Production error tracking için: https://sentry.io
# For production error tracking: https://sentry.io

# Sentry DSN (Server-side)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Sentry DSN (Client-side - tarayıcıda görünür)
# Sentry DSN (Client-side - visible in browser)
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Sentry Organization (build zamanında gerekli)
# Sentry Organization (required during build)
# SENTRY_ORG=your-org-name

# Sentry Project (build zamanında gerekli)
# Sentry Project (required during build)
# SENTRY_PROJECT=your-project-name

# Sentry Auth Token (source map upload için)
# Sentry Auth Token (for source map upload)
# SENTRY_AUTH_TOKEN=your-auth-token
